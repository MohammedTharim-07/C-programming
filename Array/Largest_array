#include<stdio.h>
int main()
{
    int arr[5];
    int largest;
    for(int i=0;i<5;i++)
    {
        printf("enter num:");
        scanf("%d",&arr[i]);
    }
    largest=arr[0];
    for(int i=0;i<5;i++)
    {
        if(arr[i]>largest)
        {
       largest=arr[i];
    }
        
    }
    printf("largest=%d",largest);
    return 0;
    
}
