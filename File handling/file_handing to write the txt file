#include <stdio.h>
#include <stdlib.h>
int main()
{
int num;
FILE *fptr;
fptr = fopen("C-program\\memory_allocation\\ADD.txt","w");
if(fptr == NULL)
{
printf("Error! opening file");
exit(1);
}
printf("Enter num: ");
scanf("%d",&num);
fprintf(fptr,"%d",num);//write
fclose(fptr);
return 0;
}
