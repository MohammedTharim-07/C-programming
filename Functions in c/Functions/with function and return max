#include<stdio.h>
int max(int a,int b)
{
   /*return (a>b)?a:b;*/
   
   if(a>b)
   {
       return a;
   }
   else if(b>a)
   {
       return b;
   }
}
int main()
{
    int result;
    result=max(10,40);
    printf("%d",result);
    return 0;
}
