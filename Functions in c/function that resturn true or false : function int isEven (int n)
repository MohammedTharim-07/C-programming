#include<stdio.h>
int isEven(int n)
{
    if(n % 2 == 0)
    return 1 ;
    else
    return 2 ;
}
int main()
{
int number;
printf("enter the number:");
scanf("%d",&number);
int result=isEven(number);
if(result==1)
printf("even\n");
else
printf("odd\n");
return 0;
}
