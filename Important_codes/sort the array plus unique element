// Online C compiler to run C program online
#include <stdio.h>

int main() {
   int n,temp,duplicate;
   printf("enter the num:");
   scanf("%d",&n);
   
   int arr[n];
   printf("Enter the lists:\n");
   for(int i=0;i<n;i++)
   {
       scanf("%d",&arr[i]);
   }
  for(int i=0;i<n;i++)
  {
      for(int j=i+1;j<n;j++)
      {
          if(arr[i]>arr[j])
          {
          temp=arr[i];
          arr[i]=arr[j];
          arr[j]=temp;
          }
      }
  }
  for(int i=0;i<n;i++)
  {
  printf("%d ",arr[i]);
  }
  
  printf("Unique element:");
  for(int i=0;i<n;i++)
  {
      duplicate=0;
      for(int j=0;j<i;j++)
      {
           if(arr[i]==arr[j])
           {
               duplicate=1;
               break;
               
           }
      }
     if(duplicate==0)
     {
         printf("%d ",arr[i]);
     }
  }
}
