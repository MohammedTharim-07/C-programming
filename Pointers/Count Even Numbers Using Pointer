#include<stdio.h>
int main()
{
    int arr[6]={3,8,5,12,7,10};
    int *p=arr;
    int evencount=0;
    for(int i=0;i<6;i++)
    {
        if(*(p+i)%2==0)
        {
        evencount++;
        }
    }
     printf("%d",evencount);
     
    
    return 0;
}
