#include<stdio.h>
int sumArray(int *array,int size)
{
  int sum=0;
  for(int i=0;i<5;i++)
  {
      sum+=*(array+i);
  }
  return sum;
}
int main()
{
    int arr[5]={1,2,3,4,5};
    int result=sumArray(arr,5);
    printf("%d",result);
    return 0;
}
