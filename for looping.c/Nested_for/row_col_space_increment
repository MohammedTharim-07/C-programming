#include<stdio.h>
int main()
{
int row,col,limit;

printf("enter number:");
scanf("%d",&limit);
for(row=0;row<=limit;row++)
{
for(col=0;col<=limit-row;col++)
{
printf(" ");
}
for(col=0;col<=row;col++)
{
printf("*");
}
printf("\n");
}


return 0;
}
