#include <stdio.h>

int main()
{
    int num;

    printf("Enter the number: ");
    scanf("%d", &num);

    do
    {
        printf("%d ", num);
        num--;
    }
    while(num <= 1);


    return 0;
}
