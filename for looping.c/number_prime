#include<stdio.h>
int main()
{
int num=0,limit=0,count=0;
printf("Enter the prime number:");
scanf("%d",&limit);


for(num=1;num<=limit;num++)
{
if( limit % num == 0 )
 count++;
 }
 if(count==2)
 {
printf("Prime number=%d",limit);
 }
 else
    printf("not a prime number!");
return 0;
}
